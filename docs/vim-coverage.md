# vim エンジン カバレッジ表 — @replit/codemirror-vim 6.3

最終更新: 2026-07-11(PLAN M1 スパイクの結果)

検証方法: `src/vim/codeMirrorVimEngine.test.ts` によるヘッドレス駆動(`Vim.handleKey` / `Vim.handleEx`)。**このテストとこの表は同期して更新すること。**

判定の凡例: ✅ 自動テスト済 or 実ブラウザ検証済(e2e/drive-m5.mjs)/ 🖥 ブラウザ確認待ち / ❌ 非対応

## エリア1: 基本移動・モード操作

| コマンド                  | 判定 | 備考                                                         |
| ------------------------- | ---- | ------------------------------------------------------------ |
| `h` `l`(左右)             | ✅   | `l` は M5 の実ブラウザ検証(e2e/drive-m5.mjs)で動作確認済み   |
| `j` `k`(上下)             | ✅   | `j` は M5 の実ブラウザ検証で動作確認済み(jsdom では駆動不可) |
| `w` `b` `e`               | ✅   |                                                              |
| `0` `$`                   | ✅   |                                                              |
| `gg` `G` `{n}G`           | ✅   |                                                              |
| `f{c}` `t{c}`             | ✅   | `;`(リピート)は未テスト(レッスン化する際に追加)              |
| `i` `a` `o` + `Esc`       | ✅   | モード遷移イベントも検証済み                                 |
| カウント(`3x` `2dd` `2G`) | ✅   |                                                              |

## エリア2: 編集オペレータ+テキストオブジェクト

| コマンド                                | 判定 | 備考                                                                                       |
| --------------------------------------- | ---- | ------------------------------------------------------------------------------------------ |
| `x` `dd` `dw`                           | ✅   |                                                                                            |
| `diw` `ciw`                             | ✅   | `ciw` の insert 突入も検証                                                                 |
| `di"` `ci"`                             | ✅   |                                                                                            |
| `yy` `p`                                | ✅   | ラインワイズのペースト位置も期待どおり                                                     |
| `Y`                                     | ✅   | **Neovim 標準(`y$`)にラッパーで再マップ済み**(素の vim の `yy` 相当ではない。domain.md P9) |
| `.`(リピート)                           | ✅   |                                                                                            |
| `v` + テキストオブジェクト + オペレータ | ✅   | visual モード遷移も検証                                                                    |

## エリア3: 検索・置換・グローバルコマンド

| コマンド    | 判定 | 備考                                                                                                                           |
| ----------- | ---- | ------------------------------------------------------------------------------------------------------------------------------ |
| `/` `n`     | ✅   | 実ブラウザ検証済み。検索ダイアログでの入力キーも keystroke に数える(view.dom のキャプチャ計測に修正済み — 2026-07-29 実測 6/6) |
| `?` `N`     | ✅   | 後方検索・逆リピート。実ブラウザ検証済み(e2e/drive-content.mjs)                                                                |
| `:s/…/…/`   | ✅   | 現在行                                                                                                                         |
| `:%s/…/…/g` | ✅   | 全体置換                                                                                                                       |
| `:g/…/d`    | ✅   | グローバルコマンド                                                                                                             |

## エリア4: レジスタ・複数ファイル

| コマンド                  | 判定           | 備考                                                               |
| ------------------------- | -------------- | ------------------------------------------------------------------ |
| `"a〜"z` 名前付きレジスタ | ✅             | `"ayy` → 別のヤンク → `"ap` で残存を検証                           |
| `"add`(レジスタへ削除)    | ✅             |                                                                    |
| `"Ayy`(大文字=追記)       | ✅             | 散らばった行の収集に使用                                           |
| `"0p`(ヤンクレジスタ)     | ✅             | dd 後も直前の yank を貼れる                                        |
| `"_d`(ブラックホール)     | ✅             | レジスタを汚さない削除                                             |
| バッファ切替・ウィンドウ  | ❌(設計どおり) | エンジンに求めない。アプリ側でバッファ切替 UI として実装(ADR-0006) |

## エリア5: 効率化の型

| コマンド        | 判定 | 備考                                                                                              |
| --------------- | ---- | ------------------------------------------------------------------------------------------------- |
| `A` `I`         | ✅   |                                                                                                   |
| `r`             | ✅   |                                                                                                   |
| `R`(上書き)     | ✅   | **実ブラウザのみ検証可** — typeText は挿入になるため jsdom 再生不可(e2e/drive-content.mjs で検証) |
| `J` `{n}J`      | ✅   |                                                                                                   |
| `ct{c}` `dt{c}` | ✅   | 目印が `:` の解答トークンは Ex 扱いになるため模範解答では使わない(replaySolution の制約)          |
| `~` `{n}~`      | ✅   |                                                                                                   |

## エリア6: 検索の達人

| コマンド | 判定 | 備考                                                                                            |
| -------- | ---- | ----------------------------------------------------------------------------------------------- |
| `/` `?`  | ✅   | ダイアログ入力のため **実ブラウザのみ検証可**(sendKey ではパターンが normal コマンドとして走る) |
| `n` `N`  | ✅   | `*` 起点ならヘッドレスでも検証可                                                                |
| `*` `#`  | ✅   | 単語境界つき一致もテスト済み                                                                    |
| `;` `,`  | ✅   | f/t のリピート                                                                                  |

## エリア8: グランドパレード(実戦総合)

基本は既習の組み合わせ。全模範解答は e2e/drive-content.mjs が実キーイベントで再生・検証する。

| コマンド    | 判定 | 備考                                                                          |
| ----------- | ---- | ----------------------------------------------------------------------------- |
| `yi"`       | ✅   | カーソルがクォートの手前でも行内の文字列を発見して中身をヤンク(s8-l13 で解禁) |
| `vi"` + `p` | ✅   | 選択に被せて貼る差し替え。無名レジスタは上書きされる(続けて貼るのは `"0p`)    |

## エリア7: ビジュアルの型

| コマンド                       | 判定 | 備考                                                                                                                                    |
| ------------------------------ | ---- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `v` + 移動 + `d`               | ✅   | 選択はカーソル位置の文字を含む(inclusive)                                                                                               |
| `V` (+`j`) + `d` / `y` / `p`   | ✅   | 行の切り取り→移動(`V d G p`)も検証。visual 中の `j` は表示行移動のため jsdom 不可 — e2e/drive-content.mjs で実ブラウザ検証              |
| `<C-v>` 矩形 + `d` / `y` / `p` | ✅   | **要 `allowMultipleSelections`**: この facet 無しだと CM が複数選択レンジを1つに潰し、矩形編集が最終行にしか効かない(2026-07-23 に修正) |
| `<C-v>` + `I` / `A`(矩形挿入)  | ❌   | エミュレータ未対応(no-op)。レッスンは d/y/p の範囲で構成すること                                                                        |

## エンジンポートの検証済み性質

- `reset()` 後は undo 履歴が前のお題に届かない(お題間の `u` 事故防止)
- キーストローク購読は Esc・モード切替キーも数える(R2)
- バッファ変更購読がクリア判定(R1)に使える粒度で発火する
- `reset()` は vim のグローバル状態(ジャンプリスト・レジスタ・検索履歴)も初期化する。前のお題のジャンプ位置が短いバッファで `G` を壊すバグを M7 の生成テストが検出したため(お題は独立、が仕様)

## 既知の挙動差(エミュレータ vs 本物の vim)

- **最終行での `dd`**: 本物は前の行末の改行ごと消えるが、エミュレータは空行を残すことがある。生成テンプレート(line-stack)は最終行の dd を避ける設計にして回避。レッスンで最終行 dd を扱う場合は要注意。

## 残タスク

- ~~🖥 項目(j/k・h/l・/検索)を M5 の実画面で確認~~ → 完了(e2e/drive-m5.mjs による自動プレイで検証)。この過程で「キーストローク計測が domEventHandlers 経由だと vim が消費したキーを取りこぼす」重大バグを発見・修正(キャプチャフェーズで計測)
- `;` `,`(f/t リピート)、`?`(後方検索)はレッスン設計時にテスト追加
